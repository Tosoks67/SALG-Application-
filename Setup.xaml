<Window x:Class="SALG.Setup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Setup"
        Height="450"
        Width="350"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Closing="Window_Close">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2.5"/>
            <Setter Property="Opacity" Value="0.75"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2.5"/>
            <Setter Property="Opacity" Value="0.75"/>
        </Style>
    </Window.Resources>
    <Border CornerRadius="10">
        <Border.Background>
            <RadialGradientBrush>
                <GradientStop Color="White"/>
                <GradientStop Color="Black" Offset="0.56"/>
            </RadialGradientBrush>
        </Border.Background>
        <Grid>
            <Image x:Name="IconFor" HorizontalAlignment="Center" VerticalAlignment="Center" Source="/DoS_dark.png" Height="200" Width="200" Margin="0,10,10,0" Panel.ZIndex="-10" Opacity="0.5"/>
            <Image x:Name="IconBack" HorizontalAlignment="Center" VerticalAlignment="Center" Source="/DoS_dark.png" Height="300" Width="300" Margin="0,10,10,0" Opacity="0.5" RenderTransformOrigin="0.5,0.5" Panel.ZIndex="-11">
                <Image.Effect>
                    <BlurEffect Radius="15" KernelType="Gaussian"/>
                </Image.Effect>
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-15"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
            <Label x:Name="txtTitle" Height="30" VerticalAlignment="Top" HorizontalContentAlignment="Center" MouseLeftButtonDown="txtTitle_MouseLeftButtonDown">SALG Setup</Label>
            <Button x:Name="btnClose" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Background="#00000000" Content="X" Foreground="White" FontSize="15" Click="btnClose_Click" BorderBrush="#00000000">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="10">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                To="Red" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                To="#00000000" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                To="DarkRed" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                                To="#00000000" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            <Grid Margin="0,30,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label>Roblox Username:</Label>
                <TextBox x:Name="txtUsername" Grid.Column="1"/>

                <Label Grid.Row="1">Current Rank:</Label>
                <ComboBox x:Name="cbxCurrentDoS" Grid.Row="1" Grid.Column="1" SelectedIndex="0" Visibility="Hidden">
                    <ComboBoxItem Content="Cadet"/>
                    <ComboBoxItem Content="Private"/>
                    <ComboBoxItem Content="Lance Corporal"/>
                    <ComboBoxItem Content="Corporal"/>
                    <ComboBoxItem Content="Sergeant"/>
                    <ComboBoxItem Content="Staff Sergeant"/>
                    <ComboBoxItem Content="Lieutenant"/>
                    <ComboBoxItem Content="Major"/>
                    <ComboBoxItem Content="Colonel"/>
                    <ComboBoxItem Content="Assistant Director of Security"/>
                </ComboBox>
                <ComboBox x:Name="cbxCurrentTRT" Grid.Row="1" Grid.Column="1" SelectedIndex="0" Visibility="Hidden">
                    <ComboBoxItem Content="Cadet"/>
                    <ComboBoxItem Content="Operative"/>
                    <ComboBoxItem Content="Sergeant"/>
                    <ComboBoxItem Content="First Sergeant"/>
                    <ComboBoxItem Content="Captain"/>
                </ComboBox>
                <ComboBox x:Name="cbxCurrentA1" Grid.Row="1" Grid.Column="1" SelectedIndex="0" Visibility="Hidden">
                    <ComboBoxItem Content="Operator"/>
                    <ComboBoxItem Content="Tactician"/>
                    <ComboBoxItem Content="Field Sergeant"/>
                    <ComboBoxItem Content="First Sergeant"/>
                    <ComboBoxItem Content="Lieutenant"/>
                    <ComboBoxItem Content="Field Commander"/>
                    <ComboBoxItem Content="Assistant Director of Task Forces"/>
                </ComboBox>
                <ComboBox x:Name="cbxCurrentE11" Grid.Row="1" Grid.Column="1" SelectedIndex="0" Visibility="Hidden">
                    <ComboBoxItem Content="Trainee"/>
                    <ComboBoxItem Content="Probationary Operative"/>
                    <ComboBoxItem Content="Operative"/>
                    <ComboBoxItem Content="Agent"/>
                    <ComboBoxItem Content="Field Sergeant"/>
                    <ComboBoxItem Content="First Sergeant"/>
                    <ComboBoxItem Content="Lieutenant"/>
                    <ComboBoxItem Content="Field Captain"/>
                </ComboBox>
                <ComboBox x:Name="cbxCurrentN7" Grid.Row="1" Grid.Column="1" SelectedIndex="0" Visibility="Hidden">
                    <ComboBoxItem Content="Recruit"/>
                    <ComboBoxItem Content="Trainee"/>
                    <ComboBoxItem Content="Probationary Operative"/>
                    <ComboBoxItem Content="Private"/>
                    <ComboBoxItem Content="Specialist"/>
                    <ComboBoxItem Content="Field Sergeant"/>
                    <ComboBoxItem Content="First Sergeant"/>
                    <ComboBoxItem Content="Lieutenant"/>
                    <ComboBoxItem Content="Field Captain"/>
                </ComboBox>

                <Label Grid.Row="2">Quota Done:</Label>
                <TextBox x:Name="numQuotaDone" Grid.Column="1" Grid.Row="2" PreviewTextInput="InputNumCheck" CommandManager.PreviewExecuted="PasteNumCheck"/>

                <Label Grid.Row="3">Total Time:</Label>
                <TextBox x:Name="numTotalTime" Grid.Column="1" Grid.Row="3" PreviewTextInput="InputNumCheck" CommandManager.PreviewExecuted="PasteNumCheck"/>

                <Label Grid.Row="4">Current Quota:</Label>
                <TextBox x:Name="numCurrentQuota" Grid.Column="1" Grid.Row="4" PreviewTextInput="InputNumCheck" CommandManager.PreviewExecuted="PasteNumCheck"/>

                <Label Grid.Row="5">Permanent Note:</Label>
                <TextBox x:Name="txtPermanentNote" Grid.Column="1" Grid.Row="5"/>

                <Button x:Name="btnSaveButton" Content="Save" Margin="5,5,100,5" Grid.Row="6" Height="30" VerticalAlignment="Bottom" BorderBrush="White" Background="Black" Foreground="White" Click="btnSaveButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="10" BorderThickness="0.5" BorderBrush="White">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                To="White" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                To="Black" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                To="Gray" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                To="Black" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)" To="Black" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetProperty="(Foreground).(SolidColorBrush.Color)" To="White" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>